{
  "name": "05-express-first-app",
  "version": "0.0.0",
  "scripts": {
    "start": "node --env-file=.env server",
    "dev": "nodemon --env-file=.env server"
  },
  "dependencies": {
    "express": "^4.18.3",
    "mongoose": "^8.2.1",
    "nodemailer": "^6.9.11"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
